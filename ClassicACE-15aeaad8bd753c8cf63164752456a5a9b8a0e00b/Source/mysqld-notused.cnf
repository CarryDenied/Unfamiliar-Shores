#
# MySQL server configuration for local development.
#
# This configuration is mounted into the mysql docker container.
#

[mysqld]
host_cache_size=0
skip_name_resolve=ON
default_authentication_plugin=mysql_native_password 
